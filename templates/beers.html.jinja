{% extends "base.html" %}
{% from "components/beer_search.html.jinja" import beer_search %}

{% block content %}
    <h4>{{ page_title }}</h4>

    <!-- Search engine -->
    {{ beer_search(beer_types) }}

    <!-- Show the data -->
    <ul>
        {% for beer in beers %}
        <li>
            {{ beer.name }} ({{ beer.type }}) {{ beer.degree }}°
        </li>
        {% endfor %}
    </ul>
{% endblock %}