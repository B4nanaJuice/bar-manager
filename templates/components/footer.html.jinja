{% macro footer() %}

<div class="footer">
    <div class="sitemap">
        <h4>Plan du site</h4>
        <ul>
        {% for endpoint in ["cocktails", "beers", "others"] %}
            <li>
                <a href="{{ url_for('public.' + endpoint) }}">{{ endpoint|capitalize() }}</a>
            </li>
        {% endfor %}
        </ul>
    </div>

    <div class="other">
        <p>Des idées ? Des retours ? J'ai rien fait pour ça, donc viens m'en parler directement.</p>
        <p>Copyright &copy; - MaxeW - 2025</p>
        <p>Fait avec amour <span class="description-light">(et un peu de temps perdu j'avoue)</span></p>
    </div>
</div>

{% endmacro %}