{% macro navbar(page_title) %}

<div class="navbar button-list">
    {% for page, endpoint in [('Cocktails', 'cocktails'), ('Bières', 'beers'), ('Autres', 'others')] %}
        <a href="{{ url_for('public.' + endpoint) }}"
           class="{% if page_title == page %}selected{% endif %}">
            {{ page }}
        </a>
    {% endfor %}
</div>

{% endmacro %}